package com.examen.plume.ui.theme

import androidx.compose.ui.graphics.Color

// Palette Principale (Violet)
val PurplePrimary = Color(0xFF6D45A8)       // Un violet plus profond et saturé
val PurpleSecondary = Color(0xFFD0BCFF)     // Un lavande clair pour le contraste

// Thème Clair
val BackgroundLight = Color(0xFFFDF7FF)       // Un blanc très légèrement teinté de violet
val SurfaceLight = Color(0xFFFEF9FC)          // Surface un peu plus blanche que le fond
val OnPrimaryLight = Color(0xFFFFFFFF)        // Texte sur la couleur primaire
val OnBackgroundLight = Color(0xFF1E1A22)     // Texte sur le fond
val OnSurfaceLight = Color(0xFF1E1A22)       // Texte sur les surfaces (cartes, etc.)

// Thème Sombre
val BackgroundDark = Color(0xFF15121C)        // Un fond charbon avec une pointe de violet
val SurfaceDark = Color(0xFF1E1A22)           // Une surface légèrement plus claire
val OnPrimaryDark = Color(0xFFFFFFFF)         // Texte sur la couleur primaire
val OnBackgroundDark = Color(0xFFE8E0F0)      // Texte sur le fond (un peu moins blanc pur)
val OnSurfaceDark = Color(0xFFE8E0F0)        // Texte sur les surfaces

// Couleurs spécifiques
val FavoriteColor = Color(0xFFFFA726)         // Orange pour l'icône de favori
val DeleteColor = Color(0xFFE53935)           // Rouge pour la suppression
